#include "main.h"

/**
 * div - div two integers
 * @a: integer first value
 * @b: integer second value
 * Return: div of a and b
 */

int div(int a, int b)
{
    if(b == 0)
        return 0;
    else
        return (a / b);
}
